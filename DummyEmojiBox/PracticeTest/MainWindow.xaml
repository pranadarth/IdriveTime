<Window x:Class="PracticeTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PracticeTest" 
        xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" >
    <Window.Resources>
        <!--<Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"                    
          Background="Transparent"
          BorderThickness="0">
                            <emoji:TextBlock Text="{TemplateBinding Content}" FontSize="24"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Cursor="Hand"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>-->
    </Window.Resources>
    <Grid>
        <StackPanel>
            <TextBox x:Name="SearchBox" Width="250" Margin="10"
                     TextChanged="SearchBox_TextChanged"/>

            <ScrollViewer Height="300" Width="300" Focusable="False">
                <Border x:Name="EmojiCanvasContainer"/>
            </ScrollViewer>

            <TextBlock x:Name="Result" Text="Result" Margin="20" FontSize="20"/>
        </StackPanel>
    </Grid>
</Window>
