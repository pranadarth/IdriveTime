 <Window x:Class="TreeView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TreeView"
        mc:Ignorable="d"
        Title="TreeViewWindow" Height="450" Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Style.Xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="{x:Type TreeViewItem}">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="Bold" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <Grid>
            <StackPanel Background="Blue" Orientation="Horizontal">
                <TextBox Name="GName" Style="{StaticResource AddTextBoxStyle}" Tag="Give Name" Width="200" Margin="10 0"/>
                <Button x:Name="AddBtn" Content="Add" Style="{StaticResource AddButtonStyle}" Height="40" Width="70"  Margin="10 0" Click="AddBtn_Click"/>
                <Button x:Name="DeleteBtn" Content="Delete" Style="{StaticResource DeleteButtonStyle}" Height="40" Width="70" Click="DeleteBtn_Click" />
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <TreeView  Name="TreeViewer" HorizontalAlignment="Left" VerticalAlignment="Top" BorderThickness="0" Margin="2">
            </TreeView>
        </Grid>
    </Grid>
</Window>
